services:
  database:
    image: postgres:14.2
    environment:
      POSTGRES_DB: argo
      POSTGRES_PASSWORD: example
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./db/data:/var/lib/postgresql/data
    ports:
      - 5432:5432
